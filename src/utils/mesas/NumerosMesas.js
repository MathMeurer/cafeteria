export function NumerosMesas({numeros}) {
  return (
    <div className="mesa-list">
      <div className="mesa-list-box">
        <h3 className="mesa-number">{numeros.mesa}</h3>
      </div>
    </div>
  );
}
